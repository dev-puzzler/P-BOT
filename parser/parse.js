const { DataUtil } = require('../cmmn/util');

const XUTIL = new DataUtil({ name : "XUTIL", debug: true });

// parse
const parse = (args) => {
    //todo, 관용 표현 옵션들을 정의하고 분류한 객체를 반환해줍니다.



    return {
        "channel" : ""
    }
}

module.exports = { parse };